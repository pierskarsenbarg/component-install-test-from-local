[project]
name = "component-install-from-local"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "pulumi>=3.0.0,<4.0.0",
    "pulumi-azure-native>=3.13.0",
    "pulumi-storage",
]

[tool.uv.sources]
pulumi-storage = { path = "sdks/storage" }


